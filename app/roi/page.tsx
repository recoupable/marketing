import { withPageMetadata } from "@/lib/seo";
import type { Metadata } from "next";
import { MarketingPage, PageSection, PageCTA, WorkCard } from "@/components/marketing-migration/ui";
import { ROICalculator } from "@/components/marketing-migration/roi-calculator";
export const metadata: Metadata=withPageMetadata({title:"AI workflow ROI planner for music companies",description:"Model the time and cost impact of improving a recurring music-business workflow. Control the assumptions, including setup and ongoing system costs.",alternates:{canonical:"/roi"}});
export default function ROIPage(){return <MarketingPage><header className="mm-tool-intro"><p className="sp-kicker">WORKFLOW ROI PLANNER</p><h1>Is the work worth improving?</h1><p>Start with one recurring task. Model the time you could free up and the costs involved, using assumptions you can change.</p></header><ROICalculator /><PageSection eyebrow="MAKE THE ESTIMATE USEFUL" title="Measure the work before the promise."><div className="sp-grid"><WorkCard number="01" title="Count the whole task." description="Include preparation, review, corrections, and the work needed to keep the system running." /><WorkCard number="02" title="Try a conservative case." description="Move the time-reduction assumption down and costs up. See whether the first build still looks useful." /><WorkCard number="03" title="Test against reality." description="Use a small set of real tasks to measure quality and time before projecting the result across your company." /></div></PageSection><PageCTA title="Let’s work through the real numbers." description="Bring one workflow and the assumptions behind it. We’ll discuss what a useful first version could look like." /></MarketingPage>;}
